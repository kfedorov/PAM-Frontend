
box: node

build:

  steps:
    # A step that executes `npm install` command
    - npm-install

    # Execute "npm build"
    - maxon/npm-run:
            script: build

deploy:
  steps:
    - heroku-deploy:
        key: $HEROKU_KEY
        user: $HEROKU_USER
        app-name: $HEROKU_APP_NAME