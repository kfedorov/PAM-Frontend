
box: node

build:

  steps:
    # A step that executes `npm install` command
    - npm-install

deploy:
  steps:
    - heroku-deploy:
        app-name: $HEROKU_APP_NAME
        key: $HEROKU_KEY
        key-name: HEROKU_KEY_PAIR
        user: $HEROKU_USER